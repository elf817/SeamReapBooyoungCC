# Copy to .env.local for local development against XAMPP.
# If XAMPP's Apache ends up on a port other than 80 (e.g. port 80 is taken
# by IIS/Skype), change the port here to match.
NEXT_PUBLIC_API_BASE=http://localhost/backend/api
